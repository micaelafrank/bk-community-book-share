function Book({title, author, description, user_id, genre}) {

    return(
        <div>
            <h1>{title}</h1>
            <h3>{author} | {genre}</h3>
            <h4>Dropbox Location: {dropbox</h4>
            <p>{description}</p>
            <p></p>
        </div>
    )
}

export default Book;